import Link from "next/link";
import { PageHero } from "@/components/page-hero";
import "./service-detail.css";

type Props={eyebrow:string;title:string;accent:string;copy:string;symptoms:string[];work:string[];faqs:[string,string][]};
export function ServiceDetail({eyebrow,title,accent,copy,symptoms,work,faqs}:Props){return <main><PageHero eyebrow={eyebrow} title={title} accent={accent} copy={copy}/><section className="section"><div className="shell detail-grid"><div><p className="section-label">When to call us</p><h2>Start with what the vehicle is doing.</h2></div><div className="tag-list">{symptoms.map(x=><span key={x}>{x}</span>)}</div></div></section><section className="section"><div className="shell"><p className="section-label">What we can help with</p><h2>Clear, practical vehicle care.</h2><div className="detail-cards">{work.map((x,i)=><article key={x}><span>0{i+1}</span><h3>{x}</h3></article>)}</div></div></section><section className="section"><div className="shell faq"><div><p className="section-label">Questions</p><h2>Useful answers before you book.</h2></div><div>{faqs.map(([q,a])=><details key={q}><summary>{q}</summary><p>{a}</p></details>)}</div></div></section><section className="cta"><div className="shell cta-inner"><h2>Tell us what your<br/><span>vehicle needs.</span></h2><Link className="btn btn-primary" href="/quote">Get a quote →</Link></div></section></main>}
